

CREATE TABLE TBL_MST_USERDETAILS (
    USERID    NUMBER GENERATED ALWAYS AS IDENTITY,
    USERNAME  VARCHAR2(15 CHAR),
    PASS_WORD  VARCHAR2(250 CHAR),
    GROUPID  NUMBER(3, 0),
    FIRSTNAME VARCHAR2(60 CHAR),
    LASTNAME  VARCHAR2(25 CHAR),
    FULLNAME  VARCHAR2(60 CHAR),
    EMAIL  VARCHAR2(60 CHAR),
    MOBILE  VARCHAR2(15 CHAR),
    ADDRESS  VARCHAR2(100 CHAR),
    CREATED_ON  DATE,
    STATUSFLAG  NUMBER(1, 0),
    CONSTRAINT PK_USERDETAILS_USERID PRIMARY KEY ( USERID )
);

/

CREATE TABLE TBL_MST_HM_HOMEDETAILS (
    HOUSE_ID    NUMBER GENERATED ALWAYS AS IDENTITY,
    OWNER_ID    NUMBER(10, 0),
    HOUSE_NAME  VARCHAR2(20 CHAR),
    TOTAL_ROOM  NUMBER(3, 0),
    TOTAL_FLOOR NUMBER(2, 0),
    ADDRESS     VARCHAR2(100 CHAR),
    CREATED_ON  DATE,
    STATUSFLAG  NUMBER(1, 0),
    CONSTRAINT PK_HOMEDETAILS_HOUSE_ID PRIMARY KEY ( HOUSE_ID )
);

/

CREATE TABLE TBL_MST_HM_ROOMDETAILS (
    ROOM_ID     NUMBER GENERATED ALWAYS AS IDENTITY,
    OWNER_ID    NUMBER(10, 0),
    HOUSE_ID    NUMBER(10, 0),
    ROOM_NO     VARCHAR2(20 CHAR),
    FLOOR_NO    VARCHAR2(20 CHAR),
    LAST_MTR_READ   NUMBER(10,0),
    UNIT_PER_PRICE  NUMBER(3,0),
    ROOM_IMG_1  VARCHAR2(50 CHAR),
    ROOM_IMG_2  VARCHAR2(50 CHAR),
    ROOM_IMG_3  VARCHAR2(50 CHAR),
    ROOM_IMG_4  VARCHAR2(50 CHAR),
    ROOM_IMG_5  VARCHAR2(50 CHAR),
    CREATED_ON  DATE,
    STATUSFLAG  NUMBER(1, 0),
    CONSTRAINT PK_ROOMDETAILS_ROOM_ID PRIMARY KEY ( ROOM_ID )
);

/

CREATE TABLE TBL_MST_HM_TENANTDETAILS (
    TENANT_ID     NUMBER GENERATED ALWAYS AS IDENTITY,
    OWNER_ID    NUMBER(10, 0),
    HOUSE_ID    NUMBER(10, 0),
    ROOM_ID     NUMBER(10, 0),
    FULL_NAME   NUMBER(10,0),
    MOBILE_NO  VARCHAR2(20 CHAR),
    ALT_MOBILE_NO  VARCHAR2(20 CHAR),
    NO_OF_MEMBER  VARCHAR2(3 CHAR),
    MEMBER_MOBILE_NO  VARCHAR2(20 CHAR),
    RENT_AMOUNT  NUMBER(10,0),
    ADV_AMOUNT  NUMBER(10,0),
    EFFECTIVE_DATE  DATE,
    AADHAR_DETAILS  VARCHAR2(50 CHAR),
    AGGREMENT_DOC  VARCHAR2(50 CHAR),
    OTHER_DOC  VARCHAR2(50 CHAR),
    CREATED_ON  DATE,
    STATUSFLAG  NUMBER(1, 0),
    CONSTRAINT PK_TENANTDETAILS_TENANT_ID PRIMARY KEY ( TENANT_ID )
);

/

CREATE TABLE TBL_REPAIR_REQUEST (
    REQUEST_ID    NUMBER GENERATED ALWAYS AS IDENTITY,
    TENANT_ID    NUMBER(10, 0),
    HOUSE_ID    NUMBER(10, 0),
    ROOM_ID     NUMBER(10, 0),    
    REQUEST_FOR  VARCHAR2(100 CHAR),
    CREATED_ON  DATE,
    ACTION_FLAG NUMBER(1, 0),
    ACTION_BY NUMBER(10, 0),
    ACTION_DATE DATE,
    REMINDER_COUNT NUMBER(2, 0),
    STATUSFLAG  NUMBER(1, 0),
    CONSTRAINT PK_REPAIR_REQUEST_REQUEST_ID PRIMARY KEY ( REQUEST_ID )
);

/

CREATE TABLE TBL_MAINTENANCE_TYPE (
    MAINTAIN_ID   NUMBER(10, 0) PRIMARY KEY,
    MAINTAIN_FOR  VARCHAR2(100 CHAR),
    STATUSFLAG    NUMBER(1, 0) DEFAULT 0
);
/
-- Insert Records
INSERT INTO TBL_MAINTENANCE_TYPE (MAINTAIN_ID, MAINTAIN_FOR, STATUSFLAG)
VALUES
  (1, 'Room Maintenance', 0),
  (2, 'Water Leakage / Water related Issue', 0),
  (3, 'Regarding Electricity', 0),
  (4, 'For Parking Issue', 0),
  (5, 'Other', 0);

/

CREATE TABLE TBL_MST_HM_PAYMENTDETAILS (
    PAYMENT_ID  NUMBER GENERATED ALWAYS AS IDENTITY,
    TENANT_ID   NUMBER(10, 0),
    HOUSE_ID    NUMBER(10, 0),
    ROOM_ID     NUMBER(10, 0),
    DUE_DATE DATE,
    RENT_AMOUNT NUMBER,
    PRV_PENDING_AMOUNT NUMBER,
    PRV_MTR_READ NUMBER,
    CURRENT_MTR_READ NUMBER,
    CURRENT_BILL NUMBER,
    TOTAL_BILL NUMBER,
    PAID_AMOUNT NUMBER,
    CUR_PENDING_AMT NUMBER,
    TAKEN_BY NUMBER(10, 0),    
    PAID_ON  DATE,
    PAID_STATUS NUMBER(1, 0),
    CREATED_ON  DATE,
    DELETEDFLAG NUMBER(1, 0),
    STATUSFLAG  NUMBER(1, 0),
    CONSTRAINT PK_PAYMENTDETAILSS_PAYMENT_ID PRIMARY KEY ( PAYMENT_ID )
);

/

CREATE TABLE TBL_MST_OTPLOG (
    OTPID    NUMBER GENERATED ALWAYS AS IDENTITY,
    USERNAME  VARCHAR2(20 CHAR),
    OTPVAL  VARCHAR2(10 CHAR),
    CREATED_ON  DATE,
    ATTEMPT  NUMBER(1, 0),
    VERIFYSTATUS  NUMBER(1, 0),
    CONSTRAINT PK_OTPLOG_OTPID PRIMARY KEY ( OTPID )
);